{
  "info": {
    "_postman_id": "03dcf87d-ad20-4b68-8c34-ccfc8cb97be9",
    "name": "Kaizen API Template",
    "description": "Template collection for Kaizen backend APIs.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "auth": {
    "type": "basic",
    "basic": [
      {
        "key": "username",
        "value": "{{basicAuthUsername}}",
        "type": "string"
      },
      {
        "key": "password",
        "value": "{{basicAuthPassword}}",
        "type": "string"
      }
    ]
  },
  "variable": [
    {
      "key": "baseUrl",
      "value": "http://localhost:8080",
      "type": "string"
    },
    {
      "key": "basicAuthUsername",
      "value": "dev-user",
      "type": "string"
    },
    {
      "key": "basicAuthPassword",
      "value": "dev-password",
      "type": "string"
    },
    {
      "key": "googleAuthorizationCode",
      "value": "replace-me",
      "type": "string"
    }
  ],
  "item": [
    {
      "name": "Health",
      "item": [
        {
          "name": "Probe Status",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/probe",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "probe"
              ]
            },
            "description": "Returns probe status for authenticated clients."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"status code is 200\", function () {",
                  "  pm.response.to.have.status(200);",
                  "});",
                  "",
                  "pm.test(\"status payload is ok\", function () {",
                  "  var body = pm.response.json();",
                  "  pm.expect(body.status).to.eql(\"ok\");",
                  "});"
                ]
              }
            }
          ],
          "response": []
        }
      ]
    },
    {
      "name": "Auth",
      "item": [
        {
          "name": "Start Google Sign-Up",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/auth/google/authorize",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "auth",
                "google",
                "authorize"
              ]
            },
            "description": "Redirects the client to Google's OAuth 2.0 consent screen for social sign-up."
          },
          "response": []
        },
        {
          "name": "Handle Google Sign-Up Callback",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/auth/google/callback?code={{googleAuthorizationCode}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "auth",
                "google",
                "callback"
              ],
              "query": [
                {
                  "key": "code",
                  "value": "{{googleAuthorizationCode}}"
                }
              ]
            },
            "description": "Exchanges Google's authorization code for tokens, creates a user account, and redirects to the configured post-signup destination."
          },
          "response": []
        }
      ]
    },
    {
      "name": "Core APIs",
      "item": []
    }
  ]
}
